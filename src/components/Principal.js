import React from 'react';
import { View, Text } from 'react-native';

const Principal = props => (
    <View style={{marginTop: 100}}>
        <Text>Tela principal da aplicação</Text>
    </View>
);

export default Principal;